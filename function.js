const ShowString = () =>{
    const message = "Hoy es jueves."
    let = countVocals = 0;
    for (let index = 0; index < message.length; index++) {
        console.log(message[i].toUpperCase());

        if(message === 'a' || message === 'e' || message === 'i' || message === 'o' || message === 'u'){
            countVocals++;
        }
    }
}
